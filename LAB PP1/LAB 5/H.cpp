#include <iostream>

using namespace std;

int main(){
    string s;
    cin >> s;
    
    bool res;
    for(int i = 0; i < s.size(); i++){
        int cnt = 0;
        if(s[i] == )
    }

    return 0;
}